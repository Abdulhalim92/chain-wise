# Требуется установленный buf (https://buf.build/docs/installation). Выполнить make generate перед сборкой.
# Вывод — в gen/ (go_package в proto задан как gen/auth, gen/orders и т.д.). Запускать из каталога contracts.
.PHONY: generate
generate:
	buf generate
