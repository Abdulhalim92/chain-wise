# Пример config.yml. Переменные окружения (PORT, GRPC_PORT, ENV, LOG_LEVEL, LOG_FORMAT, LOG_ADD_SOURCE) переопределяют значения из файла.
port: "8080"
grpc_port: "9090"
env: dev
log_level: info
log_format: json
log_add_source: false
